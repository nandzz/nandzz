// Client-safe public API for the comments feature. Server-only reads live in
// "./server". CommentsController is the panel entry point rendered by pages.
export { CommentsController } from "./components/CommentsController";
